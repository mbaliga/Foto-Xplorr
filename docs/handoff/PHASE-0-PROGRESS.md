# Phase 0 progress log

Baseline: `main` @ `5e59d56`. Branch: `claude/fylz-fotoz-complete-y60pfw` (see note below).

**Branch note:** the brief specifies branching as `claude/phase-0-defects` from `main`. This run's
harness assigns a fixed shared branch, `claude/fylz-fotoz-complete-y60pfw`, across this repo and
four others; a different branch name would leave the work unpushable through the harness's PR flow.
The branch already exists at the `main` baseline, so this is a naming substitution only — every
other instruction in the brief (one commit per item, draft PR, etc.) is followed as written.

**Master plan note (24 Sep 2026):** `docs/handoff/MASTER-PLAN.md` was added this run. Its own
kickoff line and §3 ("Phase 0 — finish the defect brief (in progress)") say to finish Phase 0 from
this brief first and that "nothing below starts until the owner merges Phase 0" — so this run
continues exactly where it was (P0-09 onward) and stops at this brief's own Phase 0 gate; Phase 1
of the master plan (the KMP core migration) is not started.

## Items

| Item | Status | Commit SHA | Notes |
|---|---|---|---|
| P0-00 | done | 406d41a | brief + progress log |
| P0-01 | done | ce17766, 08b842a, 56827f7 | `browsableAssets` added; Places/map, Calendar, Settings preview, People strip, Collections, Tag view and AlbumsScreen's Collections cover fixed — see audit table below. `GalleryProjectionV2Test` 8/8 (3 new cases) and `lintOfflineDebug` (0 errors, 69 pre-existing warnings, none new) both confirmed green against the final HEAD. |
| P0-02 | done | 072dba2 | New `media/OriginalAccess.kt` (`uriForLocationRead`, shared `openForLocationRead` retry helper). `GeoMetadataRepository` rewritten: `LocationReadOutcome`/`shouldPersist` pure decision, `parseIso6709` pure ISO 6709 parser (altitude, hemisphere and range handling), reads go through `uriForLocationRead` with the documented retry, schema bumped 2→3 (`checked_with_original`, `source_revision`, on-upgrade deletion of pre-fix "no location" rows), `indexMissing`'s batch loop no longer re-reads the whole table (in-memory delta instead — was O(n²)), new `pruneTo(allCatalogueIds)` wired into `GalleryScreen.kt`'s `onIndexLocations`. `PhotoDetailRoom.kt`'s `ImageExifDetails` gained `readOk`; a failed read now shows "Couldn't read this photo's details" and never the manual-location picker (previously indistinguishable from a confirmed absence). New `GeoPersistenceDecisionTest`, `Iso6709ParsingTest` (10 cases), `GeoMetadataRepositoryMigrationTest` (raw-SQL v2 DB → real `SQLiteOpenHelper` upgrade, asserts no-location rows dropped, located + manual rows kept). `testOfflineDebugUnitTest` green (757/757), `lintOfflineDebug` clean, `verifyOfflineManifest`/`verifyOfflineRuntimeClasspath`/`verifyOfflineSourceReferences` all pass. `compileConnectDebugKotlin` not verified this run: blocked by Maven Central rate-limiting (HTTP 429) on an uncached `org.maplibre.gl` dependency, unrelated to any file this item touches (all touched files are flavor-agnostic `main` sources; the `connect` flavor's own compile was never reached, only its dependency download was). |
| P0-03 | done | (this commit) | New `media/BitmapDecoding.kt`: `decodeUpright(context, uri, DecodeLimits)`, the app's one bitmap decode entry point. API 28+ uses `ImageDecoder` with `setTargetSize`/`ALLOCATOR_SOFTWARE`; empirically confirmed (see `BitmapDecodingImageDecoderExifTest`, real JPEG + `Orientation=6` under `@GraphicsMode(NATIVE)`) that `ImageDecoder` already applies EXIF orientation for the raster formats it decodes directly (JPEG/PNG/WebP/HEIF/GIF/BMP — everything this app's editor/share/preview paths hand it), so that path does not rotate again. Fallback (API 26-27, or any `ImageDecoder` exception): a bounds-only `BitmapFactory` pass, the largest power-of-two `inSampleSize` whose result is still ≥ target, an exact `Bitmap.createScaledBitmap`, then a `Matrix` applies the EXIF orientation read from a second stream — `BitmapFactory` has never applied it. Three pure, unit-tested helpers: `targetSize` (aspect-preserving, both a long-edge and a pixel-count limit, never upscales), `sampleSizeFor` (the classic Android sample-size search, factored out — this was the actual source of the "lands below the target" defect), `orientationTransform` (all 8 EXIF values → a rotate/flip pair). Replaced the private decode helper at all 9 call sites the brief named: `editor/EditorScreen.kt` (both the preview and the export decode — export gained the brief's own `MAX_EXPORT_PIXELS = 64_000_000L` budget alongside the existing `MAX_EXPORT_EDGE = 8192`, and the "saved a copy" message now says when and at what size a save was downscaled, which no message did before this — there was nothing to update), `share/SharePreparer.kt`, `share/ShareOptionsSheet.kt`, `viewer/PhotoDetailRoom.kt` (palette), `lift/LiftOverlay.kt`, `recognition/RecognitionIndexer.kt`, `ai/SimilarityIndexer.kt` (kept its own exact-224×224-square force-fit afterward — `decodeUpright`'s own contract is aspect-preserving only), `spatial/SpatialOpenGlScene.kt`, `experience/PhotoWallRenderer.kt`. New `media/BitmapDecodingTest.kt` (19 pure-logic cases), new `media/BitmapDecodingImageDecoderExifTest.kt` | `targetSize`/`sampleSizeFor`/`orientationTransform`: 19 cases, all 8 EXIF orientations, the brief's own 9000×6000→8192×5461 worked example, the pixel budget binding tighter than the edge budget, sample-size search bounded by the tighter axis, a degenerate zero-width source. `BitmapDecodingImageDecoderExifTest`: a real 4×6 JPEG tagged `Orientation=6`, decoded through `ImageDecoder` directly (not through this app's own code), reports and decodes as upright 6×4 — both the header callback's size and the final bitmap | Robolectric + plain JVM, `testOfflineDebugUnitTest` green (777/777 across the full suite, up from 757), `lintOfflineDebug` clean (0 errors, 69 pre-existing warnings, none new — the `ImageDecoder` calls needed a `@RequiresApi(Build.VERSION_CODES.P)` on the private function that makes them, since lint's API-level guard recognition doesn't cross a function-call boundary on its own), `assembleOfflineDebug` succeeds | The research pass that first read all 9 call sites (before any edit) swapped its own description of `spatial/SpatialOpenGlScene.kt` and `experience/PhotoWallRenderer.kt` — attributed the more defensive one (with a post-decode scale-down and an ARGB_8888 conversion) to the wrong file. Caught by re-reading each file fresh immediately before editing it, per this doc's own workflow, rather than trusting the research report's code blocks directly; no edit was made against the wrong file. `SharePreparer.kt`'s `copyBackKeptExif` used to copy the source's own `Orientation` EXIF tag onto a re-encoded, framed share — correct when the render pipeline decoded sideways pixels and left them sideways (tag says "rotate me", pixels need it), now wrong once `renderFramed` decodes upright through `decodeUpright` (pixels are already correct, so copying the old tag would tell a viewer to rotate a second time). Fixed as its own real bug, not a pre-existing one this task merely inherited: removed `Orientation` from `KEPT_EXIF_TAGS` and the function now writes `Orientation = 1` explicitly, matching `decodeUpright`'s own documented contract for every EXIF-writing caller. `RecognitionIndexer.kt`'s `InputImage.fromBitmap(bitmap, 0)` was already passing a rotation of `0` — previously an unexamined assumption for a bitmap that was not actually upright, now genuinely correct, which should improve face/text recognition accuracy on any photo with a non-identity EXIF orientation (a real, if secondary, correctness fix riding along with this task). Two files (`spatial/SpatialOpenGlScene.kt`, `experience/PhotoWallRenderer.kt`) decode on a plain background `Executor`, not a coroutine, so their call sites wrap `decodeUpright` in `runBlocking` — safe there specifically because it's already a dedicated background thread, never the caller of `requestTexture` itself. A `testOfflineDebugUnitTest` run printed a harmless `Failed to create image decoder with message 'unimplemented'` to stderr from some pre-existing test exercising a tiny synthetic bitmap outside `@GraphicsMode(NATIVE)`; `decodeUpright`'s `runCatching { decodeWithImageDecoder(...) }.getOrNull()` is exactly the designed fallback for this (API 28+ `ImageDecoder` exception → `BitmapFactory` fallback), confirmed by all 777 tests, including that one, passing with correct assertions — not a crash masked by a broad catch. |
| P0-04 | done | (this commit) | New `share/MetadataStripper.kt`: pure Kotlin (no Android import), magic-byte format detection, parses and re-emits only the segments/chunks each format's own spec guarantees carry no per-photo location or identity metadata. JPEG: keeps SOI/APP0/APP2-ICC_PROFILE/APP14-Adobe/DQT/DHT/every SOF marker/DRI/SOS+entropy data(with FF00 stuffing and FFD0-D7 restart markers handled across a second, progressive-style scan)/EOI, drops every other APPn (Exif AND XMP both live in APP1) and COM, stops at the first EOI and drops any trailer (a motion-photo MP4 tail) after it. PNG: an explicit keep-list (IHDR/PLTE/IDAT/IEND/tRNS/gAMA/cHRM/sRGB/iCCP/sBIT/pHYs/bKGD/hIST/sPLT/acTL/fcTL/fdAT) plus eXIf/tEXt/zTXt/iTXt/tIME dropped by name, any other CRITICAL (uppercase-first-letter) chunk fails closed as `Unsupported`, chunks are copied verbatim so CRCs are untouched rather than recomputed. WebP: drops EXIF/XMP RIFF chunks, clears VP8X's EXIF(0x08)/XMP(0x04) flag bits, recomputes the RIFF size from what actually ships, refuses anything over 64MB. GIF: keeps image data and the Graphic Control Extension, drops Comment Extensions, keeps an Application Extension only when its 11-byte identifier is `NETSCAPE2.0` or `ANIMEXTS1.0` (the looping convention), drops any other. BMP: copied whole (no per-photo location metadata format exists for it). Unrecognized magic bytes (HEIC/HEIF/AVIF/DNG/RAW/TIFF, the exact formats `ExifInterface.saveAttributes()` used to silently no-op on) → `Unsupported`, never guessed at. `SharePreparer.kt`: `prepare()` now returns `Result<List<PreparedItem>>` (`PreparedItem.Ready(uri, mimeType, note)` / `Failed(asset, reason)`) instead of throwing on the first item that can't be prepared — a share of 20 photos with one bad file used to fail all 20 with no indication which. The plain (no frame/watermark) share path now streams through `MetadataStripper` instead of `ExifInterface.setAttribute(tag, null) × 16`; a JPEG whose source `Orientation` wasn't 1 gets that one tag written back afterward (pixels are untouched bytes on this path, unlike the rendered path, so the rotation hint would otherwise be lost along with the rest of APP1); a format `MetadataStripper` reports `Unsupported` is decoded via `decodeUpright` (limits 8192px/48MP) and re-encoded as JPEG q95, or PNG when the source has alpha, with a `note` telling the sharer it was converted; every stripped or re-encoded JPEG/PNG/WebP output is verified before being handed out — re-read via `ExifInterface(...).latLong == null` plus a raw-byte ASCII scan for `GPSLatitude`/`GPSLongitude`/`exif:GPS`/`LocationShown`/`LocationCreated` — and becomes `Failed` rather than shared if either check doesn't pass. Also fixed, item 6 of the brief: animated-by-MIME images (`MediaAsset.isAnimated` — GIF/WebP/AVIF, the same MIME-based proxy the brief itself specifies pending P0-14's real animation index) are now routed away from `prepareRendered` even when a frame or watermark was requested — previously any GIF/WebP would have had a static frame silently baked over its animation; they are still metadata-stripped like any other image, just never rendered. `FotoXplorrActivity.kt`'s `shareWith` now shares every `Ready` item and surfaces a message naming what wasn't ("\"x.jpg\" couldn't be prepared..." / "\"x.jpg\" and N more...") instead of one hard failure for the whole batch; `commonShareType` (now `internal`, was `private`) derives the share Intent's MIME type from what was actually *produced* (each `PreparedItem.mimeType`) rather than the original assets or a `requiresRender` special case — a stamp frame's `image/png` or an unsupported-format re-encode's `image/jpeg` now flow through the same real path instead of being hardcoded around. P0-04's own described defect #4 (the rendered path copying the source's `Orientation` tag onto already-upright re-rendered pixels) was already fixed as an incidental part of P0-03's `SharePreparer.kt` change (`renderFramed`/`copyBackKeptExif` — orientation removed from `KEPT_EXIF_TAGS`, `Orientation = 1` written explicitly); re-verified rather than re-fixed here, and left alone. | New `share/MetadataStripperTest.kt` (plain JUnit, synthetic byte arrays: JPEG segment keep/drop + EOI truncation, a progressive-style second-scan entropy test with FF00 stuffing and restart markers, PNG iCCP/acTL-kept vs eXIf/iTXt-dropped vs unknown-critical-fails-closed vs unknown-ancillary-dropped, PNG missing IEND, WebP EXIF/XMP-dropped + VP8X flags cleared + RIFF size recomputed + pad-byte-preserved, WebP >64MB refused, GIF NETSCAPE-kept/comment-and-other-app-dropped, GIF missing trailer, BMP copied whole, unrecognized magic, truncated JPEG); new `share/MetadataStripperJpegTest.kt` (Robolectric `@GraphicsMode(NATIVE)` — a real `Bitmap.compress` JPEG with injected Exif/XMP/ICC/Photoshop/COM segments and a trailer strips to exactly SOI+APP0+ICC+(untouched real bytes)+EOI and still decodes to the same dimensions; a real photo with GPS + Make written via `ExifInterface` has both gone after stripping, confirmed by a structured `ExifInterface` re-read AND a raw ASCII scan, and still decodes); new `share/SharePreparerTest.kt` (Robolectric `@GraphicsMode(NATIVE)`, real JPEGs through a shadowed `ContentResolver`: a 3-item batch with one undecodable item still shares the other two and names the bad one; GPS is removed but a non-default `Orientation` is restored on the plain-strip path; `stripMetadata = false` copies the source through with its GPS untouched; a GIF asset is stripped but never rendered even with a Polaroid frame requested); new `FotoXplorrActivityShareTest.kt` (plain JUnit: `commonShareType`'s image/video/mixed narrowing, `unpreparedShareItemsMessage`'s null/one-failure/many-failures wording). `testOfflineDebugUnitTest` green (799/799, up from 777), `lintOfflineDebug` clean (0 errors, 69 pre-existing warnings, none new), `assembleOfflineDebug` succeeds. | This module's Kotlin compilation does not expose `java.desktop` (`java.awt`/`javax.imageio` are unavailable even in test sources — confirmed by a compile failure, not assumed), so the JPEG round-trip test's "real, decodable image" is a single-scan `Bitmap.compress` output with hand-injected metadata segments rather than a genuinely multi-scan progressive JPEG from a JDK encoder; the progressive/multi-scan entropy-data-scanning logic itself is instead pinned by a byte-exact synthetic-array test in `MetadataStripperTest.kt`, which needs no real codec at all. `SharePreparerTest` needed an explicit `@Before Robolectric.buildContentProvider(FileProvider::class.java).create(ProviderInfo().apply { grantUriPermissions = true })` — left to Robolectric's own manifest-driven lazy provider discovery, the first-ever `FileProvider.getUriForFile` call in a fresh sandbox threw "Failed to find configured root that contains ..." even for a file squarely inside the declared `outgoing-share` root (root-caused via a series of isolated scratch repros, not assumed); a bare `Robolectric.setupContentProvider(FileProvider::class.java, authority)` almost fixes it but then throws "Provider must grant uri permissions" since it does not carry over the manifest's `android:grantUriPermissions="true"`. No render-path (frame/watermark) equivalent of the per-item batch test was written — `SharePreparerTest`'s three real-JPEG cases all resolve a Pro entitlement (`watermark` off, default `ShareFrame.NONE`) specifically to exercise the plain copy/strip path P0-04 actually changed; `renderFramed` itself is unchanged beyond now returning a `PreparedItem`, and its own pixel/EXIF correctness is already covered by P0-03's `BitmapDecodingImageDecoderExifTest` plus this task's own animated-image test. Verification-before-handout's *failure* branch (an actual leak slipping past `MetadataStripper` and getting caught only by the re-check) is exercised only by code review, not a dedicated test — deliberately breaking the stripper just to prove the safety net catches it seemed like the wrong kind of test to add, but it is worth a reviewer's eye specifically. |
| P0-05 | done | (this commit) | New `video/StreamCopyRemuxer.kt`: `remux(context, source, target, rangeUs, rotationDegrees): RemuxResult` -- pulled the stream-copy core (track selection, keyframe/GOP-safe seek, buffer sizing, `muxerBufferFlagsFor`) out of `moments/ClipExporter.kt`, generalized to accept a nullable range (null copies the whole stream untouched) instead of always trimming. Never calls `MediaMuxer.setLocation`. New `video/Container.kt`: `containerFor(videoMime, audioMime): Container?`, a pure function checked against Android's "Supported media formats" page crossed with confirmed reports of `MediaMuxer`'s own narrower muxer-side support (its full doc text wasn't fetchable this session -- WebFetch kept returning nav-only content for the live developer.android.com pages; cross-checked via its static mirrors, the platform format table, and targeted searches instead, cited inline) -- AVC/HEVC+AAC/AMR to MP4, VP8/VP9+Vorbis/Opus to WebM, everything else (AV1 chief among them, whose `MediaMuxer`-specific support this task could not pin to an API level from available sources) to null rather than a guess. `selectPrimaryTracks(mimeTypes): PrimaryTrackSelection?`, the brief's own named track-selection function: first video + first audio track by index, dropping every other track (a second video/audio track, subtitles, Apple `mebx`-style timed metadata). New `video/VideoRotation.kt`: `findRotationDegrees(context, uri)`, also pulled out of `ClipExporter` unchanged. `ClipExporter.exportClip` now calls `remux` with its own clip range and `Result.failure`s on `RemuxResult.Unsupported`; its target file is named without an extension until `remux` reports which container it actually wrote (previously hardcoded `.mp4` always, even though nothing previously checked codec/container compatibility at all -- a VP8/VP9 clip would have thrown inside `MediaMuxer.addTrack` and failed anyway, so this is not a behavior change for any clip that used to succeed, only a widening for one that used to always fail). `SharePreparer.kt`: new `prepareRemuxedVideo`, reached when `asset.isVideo && options.stripMetadata`; remuxes into the share directory, renames from the container's own extension, then re-verifies `MediaMetadataRetriever.METADATA_KEY_LOCATION` is null on the output before handing it out (never trusting `remux` never having called `setLocation`, the same "never hand out unverified" contract images already get) -- both `RemuxResult.Unsupported` and any muxer exception become `Failed` with the brief's own actionable reason ("turn off \"Remove location and camera data\" to send the original", matching the real toggle's label in `ShareOptionsSheet.kt`). New `video/ContainerTest.kt` | `ContainerTest` (12 cases): the full `containerFor` truth table (AVC/HEVC→MP4 with AAC/AMR/no audio, VP8/VP9→WebM with Vorbis/Opus/no audio, AV1 and an unrecognized MPEG-4-SP video →null, a video/audio pair that each only fit a *different* container →null rather than either, an unrecognized audio codec →null); `selectPrimaryTracks` (first-of-each in either order, drops a subtitle/metadata track, drops a second video or audio track, a silent video has no audio index, no video track at all →null) | Robolectric + plain JVM, `testOfflineDebugUnitTest` green (810/810, up from 799), `lintOfflineDebug` clean (0 errors, 69 pre-existing warnings, none new), `assembleOfflineDebug` succeeds | No integration test actually remuxes a real video end to end: the brief's own Tests section asks only for the `containerFor` truth table and the track-selection function, both pure and covered; this sandbox has no device/emulator and no prior art in this repo (`ClipExporter` itself was never under a Robolectric-driven `MediaExtractor`/`MediaMuxer` test before this task either) to build a real-video test against, and hand-authoring a byte-valid synthetic MP4/WebM for `MediaExtractor` to parse was judged not worth the effort the brief itself doesn't ask for. `containerFor`'s exact API-level cutoffs (HEVC-in-MP4 confirmed at API 24, four below this app's `minSdk` 26, so unconditional; AV1's *muxer*-specific support level genuinely could not be confirmed from reachable sources and was excluded rather than guessed at) are worth a second look by anyone with direct access to the current `MediaMuxer` reference page. |
| P0-06 | done | (this commit) | New `share/FileProviderAuthority.kt`: `fileProviderAuthority(context) = "${context.packageName}.files"`, now the only place this string is built — `share/SharePreparer.kt`, `share/ZipExporter.kt`, `lift/StickerExporter.kt`, `moments/MomentFrameExporter.kt`, `moments/ClipExporter.kt` all call it instead of each building their own copy inline (four of the five already had the right string independently; `ZipExporter` alone had the actual bug, `"${packageName}.fileprovider"`, a name the manifest has never declared). `ZipExporter.kt` rewritten: `export(items, stripMetadata, onProgress)` now runs `items` through `SharePreparer.prepare(items, options, allowRender = false)` (see below) instead of streaming each asset's original bytes straight in, skips `Failed` items rather than writing an empty entry, and returns a new `ZipExport(uri, failed: List<PreparedItem.Failed>)` instead of a bare `Uri` so the caller can report skipped items the same way an ordinary share already does. `zip.setLevel(Deflater.NO_COMPRESSION)` set (previously only claimed in a comment). New `archiveName(count, includesVideo)` (was `archiveName(items)`) says "item(s)" instead of "photo(s)" when the ORIGINAL selection included any video, and counts what the archive actually holds (`ready.size`), not the original selection size, so a partially-failed batch never claims more entries than it delivers. New `entryNameFor(originalDisplayName, item)`: keeps the source's own display name only when its extension still matches what the prepared item actually decodes as (via `MimeTypeMap`), and replaces it otherwise — a real, if quiet, correctness gap the old code had for any re-encoded item (e.g. an unsupported HEIC stripped by re-encoding to PNG) that this task's own new "entries come from `SharePreparer`" design would otherwise have reintroduced under a stale extension. `share/SharePreparer.kt` gained an `allowRender: Boolean = true` parameter on `prepare()` (default preserves every existing caller/test unchanged): when false, no item is ever routed through `prepareRendered`, regardless of `ShareOptions` or Pro status. `FotoXplorrActivity.kt`'s `onExportZip` now passes `preferences.toShareOptions().stripMetadata` and reports `export.failed` via the existing `unpreparedShareItemsMessage` (whose doc comment now names both callers). New `share/FileProviderAuthorityTest.kt` (modeled on `ShareDirectoryTest`), new `share/ZipExporterTest.kt` | `FileProviderAuthorityTest`: the FileProvider `<provider>` in `AndroidManifest.xml` declares exactly `${applicationId}.files`. `ZipExporterTest` (13 cases): `archiveName`'s pluralization/wording truth table; `uniqueName`'s first-use-as-is and numbered-collision behavior (no prior test existed for either, despite predating this task); `entryNameFor` keeping a correctly-typed name, replacing a stale extension, and falling back when `MimeTypeMap` doesn't know the type; a failed item is skipped and reported, never written as an empty entry; an all-failed batch fails the whole export rather than producing an empty archive; an empty selection is refused; entries follow the caller's `stripMetadata` choice in both directions (GPS present/absent); the archive name says "item" for a video-inclusive selection even when that specific video item itself fails; entries are written with `NO_COMPRESSION` even for maximally-compressible content (asserted via `ZipFile`'s central-directory `compressedSize`, not `ZipInputStream`, which reports `-1` for a streamed entry's sizes until fully read past) | Robolectric + plain JVM, `testOfflineDebugUnitTest` green (824/824, up from 810), `lintOfflineDebug` clean (0 errors, 69 pre-existing warnings, none new), `assembleOfflineDebug` succeeds | **A real, previously-latent bug found and fixed while writing this task's own tests, not routed around:** `SharePreparer.prepare` unconditionally re-resolves `ShareOptions.watermark` against the caller's live Pro status (`resolvedFor`, a deliberate anti-bypass design — see that class's own doc — so no caller, UI or otherwise, can construct `ShareOptions(watermark = false)` and skip the mark for a non-Pro sharer). `ZipExporter` explicitly requesting `watermark = false` therefore did nothing for a non-Pro user: the mark was still resolved on, forcing a render, which for a "keep metadata" zip export additionally meant GPS silently went missing too (`renderFramed`'s kept-metadata path only ever copies back `TAG_DATETIME_ORIGINAL`, never GPS) — directly contradicting this task's own "plain path only: never a frame or watermark" requirement, and, for the metadata-kept case, an actual privacy-direction bug in the opposite way from P0-04's (dropping data the user asked to keep, rather than keeping data they asked to drop). Caught by `ZipExporterTest`'s own "keep metadata" case failing before any fix was written, exactly per this session's own failing-test-first discipline. Fixed with the new `allowRender` parameter rather than trying to bypass `resolvedFor` from outside `SharePreparer` (which has no seam for that at all): it is a narrow, class-internal-only seam that leaves the anti-bypass invariant fully intact for every existing caller (the plain `Share` action, the advanced sheet, every existing test), and is reachable only from within `share/`, not through `ShareOptions` itself where an ordinary caller could reach it. Owner question 4 below, since the brief's original "never a frame or watermark" line for zip predates the watermark/Pro-tier decision by about a week and could plausibly have meant something narrower once that decision existed. `MimeTypeMap` needed explicit `ShadowMimeTypeMap.addExtensionMimeTypeMapping` calls in the test to behave like a real device at all — confirmed empirically that Robolectric's shadow starts with no built-in mime/extension table, unlike the real platform class. |
| P0-07 | done | (this commit) | New `metadata/ExifCopier.kt` gained two things beyond what it already had (its `COPIED_TAGS`/`EXCLUDED_TAGS`/`allExifTags`/`resetXmpOrientationForCopy` already existed from earlier this session, untested until now): `copiedXmpValue(rawXmp)` (no source XMP → null; parses → `resetXmpOrientationForCopy`'s adjusted packet, re-serialized; does not parse at all → the raw string unchanged, per the brief's own three-way XMP rule) and `copy(source, target)` extended to also reset `TAG_ORIENTATION` to 1 and copy XMP via `copiedXmpValue`, not just the tag set — one call now does everything an edited copy's metadata needs. `EXCLUDED_TAGS`'s `TAG_THUMBNAIL_ORIENTATION` entry replaced with its own literal string `"ThumbnailOrientation"`: the constant is `@RestrictTo`d to androidx's own library group (a real `lintOfflineDebug` `RestrictedApi` error, caught by actually running lint rather than assumed clean) — `allExifTags()`'s reflection still finds the field by value regardless. New `media/MediaStoreDestination.kt`: `MediaKind` (IMAGE/VIDEO/AUDIO), `mediaStoreRelativePath(sourceRelativePath, kind, sdkInt = Build.VERSION.SDK_INT)` — source's own top-level folder kept verbatim when allowed for that collection, else `Pictures|Movies|Music/Foto Xplorr/`; allowed lists exactly as the brief states, cross-checked against developer.android.com's shared-storage page (cited inline; `Recordings` gated on API 31 per that page's own "isn't available on Android 11 (API level 30) and lower" line) — `sdkInt` is a parameter, not a raw `Build.VERSION.SDK_INT` read, purely so the API-31 gate is testable in a device-less sandbox that has only one Robolectric framework jar cached offline (see Decisions). `editor/EditedCopyWriter.kt` rewritten: both `save` and `overwrite` now encode to a new `cacheDir/edit-staging/` temp file first, call a new private `applySourceMetadata` (opens the source through `uriForLocationRead`/`openForLocationRead` so GPS survives Android 10+ redaction, then `ExifCopier.copy` onto the temp file — wrapped in `runCatching`, best-effort, never fails the save/overwrite itself) before either MediaStore-inserting or truncating anything. `save` now sets `DATE_TAKEN = source.dateTakenMillis` on insert, uses `mediaStoreRelativePath(source.relativePath, MediaKind.IMAGE)` instead of reusing the source path unconditionally, and re-queries/re-asserts `DATE_TAKEN` once after clearing `IS_PENDING` in case the platform's own rescan changed it. `overwrite` reads and applies metadata to the temp file BEFORE the original is touched at all, verifies the temp file decodes (bounds-only) at the edited bitmap's own dimensions, then streams it into the original opened as `"wt"` (write+truncate, no read — the brief's own exact mode, not the previous code's `"rwt"`). `video/VideoConversionWriter.kt`: `RELATIVE_PATH` now goes through `mediaStoreRelativePath(source.relativePath, MediaKind.VIDEO)`, `DATE_TAKEN` now set from `source.dateTakenMillis` (previously not set at all). `audio/AudioConversionWriter.kt`: now sets `RELATIVE_PATH` via `mediaStoreRelativePath(source.relativePath, MediaKind.AUDIO)` — previously did not set `RELATIVE_PATH` at all, because `AudioAsset` carried no `relativePath` field; added one (`String? = null`, defaulted for the two existing construction sites' compatibility) and wired `AndroidAudioMediaStoreScanner`'s projection/cursor-columns to populate it (API 29+ only, matching the photo/video scanner's own existing `RELATIVE_PATH` gate). New `metadata/ExifCopierCompletenessTest.kt`, `metadata/ExifCopierTest.kt`, `media/MediaStoreDestinationTest.kt` | `ExifCopierCompletenessTest` (2 cases): every real `TAG_*` field is in exactly one of `COPIED_TAGS`/`EXCLUDED_TAGS`, and `EXCLUDED_TAGS` names no non-existent tag. `ExifCopierTest` (3 cases, Robolectric `@GraphicsMode(NATIVE)`, real JPEGs on disk by file path — the brief's own named shape): a source with GPS/DateTimeOriginal/Make/Model/XMP `dc:subject`/Orientation 6 copied onto a differently-sized target has all of it, `Orientation` reset to 1 (not copied), and the target's own (not source's) dimensions on decode; an XMP `tiff:Orientation` is reset to 1 rather than copied verbatim; a source with no XMP at all leaves the copy with none, not an invented empty packet. `MediaStoreDestinationTest` (14 cases): every case bullet from the brief's Tests section (`DCIM/Camera/`, `Pictures/Screenshots/`, `Download/`, the WhatsApp path, `null`, `Movies/` for video vs. falling back for images, `Documents/` for all three collections), plus `DCIM` allowed for video too, every other audio folder, the `Recordings` API-31 gate both sides via the `sdkInt` parameter, and a blank (not just null) source path falling back | Robolectric + plain JVM, `testOfflineDebugUnitTest` green (843/843, up from 824), `lintOfflineDebug` clean (0 errors, 69 pre-existing warnings, none new — including after the `RestrictedApi` fix above), `assembleOfflineDebug` succeeds, and `./scripts/verify.sh` (both flavors, all three offline enforcement gates) reports `VERIFY OK` against this commit's parent | No MediaStore-level test exercises `EditedCopyWriter.save`/`overwrite` end to end (insert, `IS_PENDING`, the `DATE_TAKEN` re-assert, the temp-file lifecycle) — the brief's own Tests section for this item names only the three cases above (`ExifCopier` completeness, the file-path round trip, `mediaStoreRelativePath`), all pure or file-level and none needing a `ContentResolver`; a `ShadowContentResolver`-backed MediaStore test was judged out of this item's stated scope rather than skipped by oversight. |
| P0-08 | done | (this commit) | **Item 1 — writable check before consent.** New `metadata/WritableFormat.kt`: `isMetadataWritable(resolver, asset)` sniffs real magic bytes (JPEG/PNG/WebP), reusing `share/MetadataStripper.kt`'s own `isJpeg`/`isPng`/`isWebp` (widened `private`→`internal`) rather than a second hand-typed copy, falling back to `asset.mimeType` only when the byte read itself fails. `FotoXplorrActivity.kt`'s `requestMetadataWrite` (the one choke point every caption/rating/keyword/location edit already went through) now checks it first, before `pendingMetadataAsset`/`Edit` are even set or `MediaStore.createWriteRequest` is called, showing "Foto Xplorr can't write metadata into *FORMAT* files yet" and refusing rather than opening consent only to fail. `writeBatch` (the brief's "bulk metadata edits" surface) has no UI caller anywhere in this codebase yet — nothing to wire it into; noted under Decisions. **Item 2 — original access for writes.** `MetadataWriter.write` now refuses outright on API 29+ when `uriForLocationRead` reports it could not get the unredacted view (permission not granted) — writing through a redacted view risks `ExifInterface`'s own read-modify-write cycle silently erasing GPS tags the edit never touched, confirmed as a real platform mechanism (not assumed) via `RedactingFileDescriptor`'s own AOSP javadoc, logged under Decisions with the source. Otherwise opens through the same `uriForLocationRead`/`setRequireOriginal` P0-02 already built, reused rather than reinvented. **Item 3 — temp, verify, then write.** `write` now stages the original's bytes into `cacheDir/metadata-staging/`, edits ONLY the temp file, verifies it (bounds unchanged before/after, and a FRESH, independently-opened `ExifInterface` re-read confirms every field the edit asked for actually landed), then streams the verified temp file into the original opened `"wt"` — mirrors P0-07's `EditedCopyWriter.overwrite` shape exactly. Remaining risk (interruption mid final-stream) documented in the class's own KDoc, per the brief's explicit ask. **Item 4 — UTF-8 XMP, test written first.** New `Utf8XmpPreservationTest.kt`: a real JPEG with genuine (non-entity-escaped) UTF-8 XMP (`dc:subject` "Zürich"/"東京"/"José") — **failed against the original code**, `ExifInterface.getAttribute(TAG_XMP)` decoded them as replacement characters. Fixed with a new `ExifInterface.xmpAttributeUtf8()` extension (`XmpPacket.kt`) that decodes `getAttributeBytes(TAG_XMP)` as UTF-8 itself rather than trusting androidx's own (wrong) decode; `readXmpAttribute` and P0-07's `ExifCopier.copy` both switched to it. The write side needed no fix: every packet this app itself produces is already pure ASCII (`XmpPacket.serialize`'s numeric-character-reference escaping), so `setAttribute` was never the risk. **Item 5 — clear location clears XMP GPS too.** New `XmpPacket.clearProperty` (any shape, element or attribute-form, without needing to read the value first) plus new `EXIF_NS`/`IPTC4XMPEXT_NS` constants; `applyToXmp` now clears every `exif:GPS*` property plus `Iptc4xmpExt:LocationShown`/`LocationCreated` when `clearLocation && setLocation == null`. Deliberately does NOT touch `photoshop:City`/`State`/`Country` — this app has no way to tell a geocoded value from a hand-typed one, so the fail-closed choice (brief 0.5) is to leave all three alone unconditionally; documented in `clearXmpLocation`'s own KDoc. **Item 6 — one merged pending edit.** New `MetadataEdit.merge(other)`: later non-null fields win, `keywordsToAdd` unions, `setLocation`/`clearLocation` merge as one combined intent (an edit that touches neither leaves the other's location intent alone). `FotoXplorrActivity.kt`: `requestMetadataWrite` is now a forward-referenced `var` lambda (so it and a new `finishMetadataWrite` can call each other) guarded by a new `metadataWriteInFlight` flag — a second edit arriving while one is already running (in flight or awaiting consent) queues, merging into anything already queued for the SAME asset, and fires as a fresh `requestMetadataWrite` call once the in-flight one finishes, fixing the concrete race `PhotoDetailRoom.kt`'s independent creator/copyright field commits caused. New `metadata/ExifCopierCompletenessTest.kt`-adjacent `metadata/MetadataEditTest.kt`, `metadata/WritableFormatTest.kt`, `metadata/MetadataWriterWriteTest.kt`, `metadata/Utf8XmpPreservationTest.kt`, plus new cases in `metadata/MetadataWriterTest.kt` | `MetadataEditTest` (8 cases): merge's later-wins/union/location-combined-intent rules, including the "later edit that never touched location" case that would break with a naive `other.clearLocation` overwrite. `WritableFormatTest` (6 cases): JPEG/PNG/WebP magic bytes writable regardless of claimed MIME, a HEIC-shaped header not writable even under a false `image/jpeg` claim, a failed byte read falling back to MIME correctly both ways. `MetadataWriterWriteTest` (3 cases — see Decisions for what this deliberately does NOT cover): staging cleans up when the staged bytes are not a real image, the `ACCESS_MEDIA_LOCATION` refusal happens before touching the file at all, and a no-op edit succeeds without even checking the permission. `Utf8XmpPreservationTest` (3 cases): the real-file round trip through the fix, a dedicated pin of the raw `getAttribute` defect itself, and `ExifCopier.copy`'s own UTF-8 correctness. `MetadataWriterTest` gained 2 cases: XMP GPS cleared alongside EXIF GPS, and photoshop:City left untouched | Robolectric + plain JVM, `testOfflineDebugUnitTest` green (866/866, up from 843), `lintOfflineDebug` clean (0 errors, 69 pre-existing warnings, none new — including after fixing a real `NewApi` finding, `InputStream.readNBytes` needing API 33 against this app's minSdk 26, replaced with a manual read loop), `assembleOfflineDebug` succeeds, `./scripts/verify.sh` (both flavors, all three offline enforcement gates) reports `VERIFY OK` | No end-to-end MediaStore write round trip is tested — confirmed empirically (not assumed) that this Robolectric version's `ShadowContentResolver` has no fixture for `openFileDescriptor(uri, "wt")` against a fabricated `content://media/...` uri (`FileNotFoundException: No content provider`), the same sandbox limitation P0-07's `EditedCopyWriter.save`/`overwrite` already hit; `registerInputStreamSupplier` (reused from `SharePreparerTest`/`ZipExporterTest`, extended to also register `MediaStore.setRequireOriginal(uri)`'s rewritten uri, since that's a genuinely different `Uri` value once `ACCESS_MEDIA_LOCATION` is granted) covers reads, which is what the refusal and staging-cleanup tests actually needed. A genuine successful write is a P0-19 device check instead. `writeBatch`/"bulk metadata edits" has no UI entry point in this codebase to wire item 1's check into. |
| P0-09 | done | (this commit) | **Runtime survives the Activity.** New `LibraryRuntime.kt`: a process-wide singleton (`LibraryRuntime.get(context)`, the exact double-checked-locking shape `organize/LibraryStore.get` already uses in this codebase — `@Volatile` instance, `synchronized(this)` double-check, constructed from `context.applicationContext`) owning the single `SqliteMediaRepository`/`MediaIndexer` pair for photo/video and the matching pair for audio, a conflated scan-request channel per media type consumed by a `scope.launch` loop started once from `init`, the `scanState: StateFlow<ScanState>` (moved here verbatim from the Activity, along with `ScanState`'s own sealed-interface definition), the `MediaStoreChangeObserver` subscription (registered exactly once via `ensureChangeObserverRegistered()`, `@Synchronized` guarded), `ensureInitialScan()`/`ensureInitialAudioScan()` (each runs the first scan once per process, also `@Synchronized` guarded), and `requestScan(userRequested)`/`requestAudioScan(userRequested)`. A rotation, multi-window resize, or any other config change that used to destroy and recreate the Activity — and with it every in-flight scan, the whole `SqliteMediaRepository` instance, and the `MediaStoreChangeObserver` registration — now recreates only the Activity; the runtime and everything it owns lives on. `FotoXplorrActivity.kt` now does `val runtime = remember { LibraryRuntime.get(applicationContext) }` and reads `repository`/`audioRepository` off it instead of constructing its own; all 17 `scanRequests.trySend(x)`/`audioScanRequests.trySend(x)` call sites become `runtime.requestScan(x)`/`runtime.requestAudioScan(x)`; the old per-composable `LaunchedEffect` scan-consumer-loop and change-observer-registration blocks (two pairs, one each for photo/video and audio) collapse to one `LaunchedEffect(permissionGranted) { if (!permissionGranted) return@LaunchedEffect; runtime.ensureChangeObserverRegistered(); runtime.ensureInitialScan(); runtime.ensureInitialAudioScan() }`. `LibraryBackgroundWork.kt`'s `runPendingWork()` now calls `LibraryRuntime.get(appContext).repository.awaitLoaded()` instead of constructing its own throwaway `SqliteMediaRepository`, so the background job and the foreground Activity are guaranteed to observe the same catalogue rather than two independent SQLite connections that could each believe they were the first to load. **Sweep, not rewrite.** `MediaRepository.replaceAll(items)` (a wholesale delete-everything-then-reinsert, which briefly leaves the UI observing an empty or partial catalogue on every single full pass — including a plain rotation forcing a rescan before this item) is replaced by `suspend fun removeAllExcept(keep: Set<MediaId>)`. `SqliteMediaRepository`'s implementation reads every id in the table, computes the complement of `keep` in Kotlin, and deletes only that complement through the same `SQLITE_BIND_LIMIT`-chunked pattern `remove()` already used — an unchanged row is now never touched at all, not deleted-then-reinserted. `InMemoryMediaRepository` (confirmed via a repo-wide grep to have zero constructor call sites anywhere in this codebase — genuinely dead code, left structurally in place rather than deleted since removing unused code is a different, unrequested task) got the same interface method. The delta-scan branch of `MediaIndexer.refresh` is unchanged and still deliberately calls neither method, per TRAPS #1. **Sweep guard (TRAPS #8).** New pure `media/SweepPolicy.kt`: `shouldSweep(catalogueSize, missingCount, userRequested, partialAccess): Boolean` — never sweeps under partial media access regardless of anything else; always sweeps on a user-requested refresh outside partial access; an unattended (background/automatic) full pass sweeps only when `missingCount <= max(200, catalogueSize / 4)`, so a suspiciously large deletion (the kind a broken or partial enumeration would produce, not a real bulk-delete a user actually did) is refused rather than silently applied. `MediaIndexer`'s constructor now takes `sweepPolicy: SweepPolicy = SweepPolicy` and `onSweepRefused: (catalogueSize: Int, missingCount: Int) -> Unit = { _, _ -> }` (a callback rather than a direct `android.util.Log` call, because `MediaIndexerTest` is plain JUnit with `testOptions.unitTests.isReturnDefaultValues` NOT set in `app/build.gradle.kts` — confirmed by reading it directly — so an unmocked `Log` call would throw `RuntimeException: Method ... not mocked` in every existing test; the real Android-Log-writing implementation is supplied only by `LibraryRuntime`, which is never itself exercised under plain-JUnit); a refusal now logs and leaves every row in place instead of running `removeAllExcept`. **Process death.** `.FotoXplorrActivity` gained `android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboard|keyboardHidden|navigation|uiMode|density"` in the manifest (confirmed nothing in this Activity reads configuration-qualified resources at creation, so suppressing the recreation is safe) so the common rotation case no longer destroys the Activity at all; for the process-death case the platform can still force regardless, the open viewer and the in-progress editor now survive via `rememberSaveable`. New `MediaIdSaver = Saver<MediaId?, Long>(save = { it?.value ?: NO_MEDIA_ID }, restore = { if (it == NO_MEDIA_ID) null else MediaId(it) })` (`NO_MEDIA_ID = -1L`, safe since MediaStore's own `_ID` is never negative) makes the inline value class `MediaId` Bundle-saveable; `selectedAssetId` and the new `editingAssetId` both use it via `rememberSaveable(stateSaver = MediaIdSaver)`. The open viewer's asset list is now `viewerAssetIds: LongArray` (rememberSaveable natively) with `viewerAssets` derived by looking each id up in the freshly-reloaded `assets` list (`viewerAssetIds.map { viewerAssetsById[it] }.filterNotNull()` — a real Kotlin stdlib gap surfaced while writing this: `LongArray.mapNotNull` does not exist as an extension the way `Iterable<T>.mapNotNull` does in this project's stdlib configuration, "Unresolved reference", so `.map { }.filterNotNull()` is used instead) rather than storing `MediaAsset` objects directly, so the viewer reopens against live, reloaded data rather than a stale saved snapshot; the editor's target asset is now `editingAssetId` with `editingAsset` derived the same way. New `media/SweepPolicyTest.kt` (9 cases), updated `media/MediaIndexerTest.kt` (renamed the `replaceAll`-era tests to `removeAllExcept`/sweep terminology; added "a full pass removes only the missing ids" and "a guard refusal leaves every row in place", the latter using `userRequested = false` with a zero/invalid watermark to reach the one realistic way an unattended pass ever plans Full at all against a large existing catalogue), new `media/SqliteMediaRepositoryTest.kt` (3 cases, `@RunWith(RobolectricTestRunner::class)`, against the real disk-backed SQLite database rather than a fake: mirror/database agreement verified via a SECOND, independently-constructed repository instance that reads only from disk; a chunk-spanning case at 2,500 rows crossing the 900-row `SQLITE_BIND_LIMIT`; keeping everything is a no-op) | Robolectric + plain JVM, `testOfflineDebugUnitTest` green (880/880, up from 866), `lintOfflineDebug` clean, `assembleOfflineDebug` succeeds, and `./scripts/verify.sh` (both flavors, all three offline enforcement gates) reports `VERIFY OK` against this commit's parent. **Deviations, stated rather than hidden.** Writing `SqliteMediaRepositoryTest`'s three cases exposed a genuine PRE-EXISTING race in `SqliteMediaRepository`, not introduced by this item: the constructor's `initialLoad = scope.launch { state.value = helper.readAll() }` is not mutex-guarded against a concurrent `upsert`/`removeAllExcept` call, so a freshly-constructed instance mutated immediately (exactly what the new tests do) can have the delayed background read overwrite a real, already-applied mutation with a stale empty-database snapshot, non-deterministically. Confirmed via repeated `--rerun` runs before and after the fix. Fixing the race itself in production code is out of this item's stated scope, so it is worked around at the test level only — `repository.awaitLoaded()` is called immediately after construction, before any mutation, in every new test — and left here as a flagged gap for whichever future item touches `SqliteMediaRepository`'s construction path. |
| P0-10 | todo | | |
| P0-11 | todo | | |
| P0-12 | todo | | |
| P0-13 | todo | | |
| P0-14 | todo | | |
| P0-15 | todo | | |
| P0-16 | todo | | |
| P0-17 | todo | | |
| P0-18 | todo | | |
| P0-19 | todo | | |
| P0-20 | todo | stretch — only if everything else is done | |

## P0-01 consumer audit

`browsableAssets(assets, archivedIds, sensitiveIds, lockedFolders, unlockedFolders, hideSensitive)`
added to `gallery/GalleryProjection.kt` as the single "may be shown right now" rule; `everydayAssets`
re-expressed on top of it (same predicates, different order of application — `.filter` preserves
input order regardless, so this is not expected to move any golden, and none did on manual review).

| Consumer | Symbol | Before | Fix |
|---|---|---|---|
| `gallery/GalleryScreen.kt:317` | `spatialAssets` (fed Places/map/compass/3D scenes AND `geoRepository.indexMissing`) | `state.assets.filterNot { it.isTrashed }` for both | Split into `spatialIndexInput` (unchanged — an indexer must see locked/archived items so unlocking never needs a re-index) and `spatialDisplayAssets` (now `browsableAssets(...)`) for what's actually drawn |
| `gallery/DestinationBrowserScreen.kt` `CalendarScreen` call | Calendar | `state.assets` (raw) | `calendarAssets`, a `remember`ed `browsableAssets(...)` |
| `gallery/SettingsTabs.kt` `sampleAsset` | Settings preview photo | `state.assets.firstOrNull { !it.isVideo }` | `browsableAssets(...).firstOrNull { !it.isVideo }` |
| `gallery/DestinationBrowserScreen.kt` `PeopleStrip` call | People-strip cover thumbnails | `assets = state.assets` (raw) even though the grid right below it in the same composable already receives the filtered `assets` parameter | Now passes the same already-filtered `assets` parameter — a real bug found during the audit, not just the three cases the brief named |
| `gallery/GalleryScreen.kt` `BrowserRoute.Collection` branch | Opening a collection | Checked membership + not-trashed only; never checked locked folders at all | Added `asset.isPrivacyVisible(lockedFolders, unlockedFolders)` — archived/sensitive items stay visible in a collection deliberately (a collection is a curated list, not the timeline), but a locked folder is never optional. Another bug found during the audit. |
| `gallery/DestinationBrowserScreen.kt` `destinationAssets()` | Every rail destination (Photos/Videos/Pets/People/Identity/Favourites/Screenshots) | Already routes through `everydayAssets`/`smartAlbumAssets`, both privacy-safe | No change needed |
| `gallery/GalleryScreen.kt` `assetsForAlbum`/`smartAlbumAssets` call sites (device albums, smart albums, Discover summaries) | — | Already privacy-safe (`isPrivacyVisible` applied inside `GalleryProjection.kt`) | No change needed |
| `gallery/GalleryScreen.kt` `BrowserRoute.Tag` branch | Opening a `#tag` view | Checked tag membership + not-trashed only; missing the same locked-folder check `Collection` (directly above it) had just been given | Added the identical `asset.isPrivacyVisible(lockedFolders, unlockedFolders)` check — same reasoning as `Collection`. Found by the background audit as a likely missed spot in the same edit. |
| `gallery/GalleryContent.kt` `AlbumsScreen` | Device-folder grid's own filtering was fine (trashed + archived, plus already-correct `cover = null` locked-folder handling), but the **Collections** section's cover lookup read the raw, unfiltered `assets` parameter directly | `AlbumsScreen` now takes `sensitiveIds`/`hideSensitive`, builds a `browsable` list via `browsableAssets(...)`, and the collection cover lookup (`collection.mediaIds.firstNotNullOfOrNull { ... }`) reads from `browsable` instead of raw `assets`. Device-folder `available` is left as its own (trashed+archived+video-pref only) list, since locked folders are already handled per-album and a collection's own archived/sensitive members are still meant to show (matching the `Collection` route's own rule) — only the *cover thumbnail* needed the fix, so a locked folder's photo can never surface as a collection's cover art. Found by the background audit. |
| `gallery/DestinationBrowserScreen.kt` `LIBRARY` `trashCount` | Library screen trash count | `state.assets.count { it.isTrashed }` | Left as-is with a one-line comment: a count, not a content display — a locked folder's trashed items still count toward "how many things are in Trash" |
| `gallery/DestinationBrowserScreen.kt` `TIDY_UP` `archiveReviewItems` | Archive-suggestion review queue | Already filters `isPrivacyVisible` (pre-existing, documented at the call site) | No change |
| `gallery/DestinationBrowserScreen.kt` `DISCOVER` `smartAlbumSummaries` | Smart-album counts/covers | Needs the full set to compute each album's own definition correctly (e.g. the Trash/Archived albums show exactly those); privacy-filtered internally per album via `smartAlbumAssets` | No change |
| `spatial/GeoMetadataRepository.kt` `indexMissing`/`clearAndReindex`, `recognition/RecognitionIndexer.kt` `index`, `recognition/RecognitionStore.kt` `pendingAssets`, `curate/AutoCurationPass.kt` `run`, `LibraryBackgroundWork.kt`, `FotoXplorrActivity.kt`'s recognition/auto-curation triggers | Background indexers/passes | Deliberately unfiltered beyond their own video/trash checks (and, for `AutoCurationPass`, an explicit locked-folder skip documented in its own KDoc) | No change — textbook "indexer must see everything so unlocking never needs a re-index" case |
| `spatial/PlacesScreen.kt`, `SpatialComposition.kt`, `SpatialPhotoLayout.kt`, `SpatialPhotoSceneScreen.kt`, `StampMapScreen.kt`, `experience/OpenGlPhotoScene.kt`, `PhotoSceneModels.kt`, `GalleryPreviewScreen.kt`, `ai/SimilarityExplorerScreen.kt` | Places/map, 3D scenes, photo wall, similarity map | All read `LocalSpatialExperience.assets`, i.e. the `spatialDisplayAssets` fixed above | Already fixed transitively; several have redundant local `!isTrashed` filters on top, harmless |
| `viewer/FilmstripScrubber.kt`, `gallery/GalleryContent.kt` `TimelineScreen`/`MediaGridScreen`, `TimelineStops.kt`, `gallery/GalleryActionsRoom.kt`, `GalleryDialogs.kt`, `fileops/MediaFileOperations.kt`, `metadata/MetadataWriter.kt`, `experience/PhotoSceneModels.kt` | Various pure display/selection components | Pure pass-throughs or explicit-selection operations, no filtering of their own | No change needed — correctness depends on (now-fixed) callers |
| `gallery/GalleryInfoRoom.kt` `state.assets.size` under "Everything" | Library-wide total count | Deliberately raw | No change — a total-count display, the audit's own worked example of a legitimate exception |
| `search/GallerySearch.kt` | — | Confirmed dead code — zero call sites anywhere in the app; live search goes through `MediaAsset.matchesGallerySearch` in `GalleryScreen.kt`, always applied on top of an already-filtered list | No change; flagged so nobody assumes this file is live |

Two items were surfaced as **product decisions, not bugs**, and are left as-is pending an owner call:

- `ai/SimilarityIndexer.kt`/`ai/EmbeddingRepository.kt`'s only caller (`SimilarityExplorerScreen`) now feeds them the filtered `spatialDisplayAssets` list, so — unlike the other indexers in this app, which deliberately index locked/archived/sensitive content — the similarity index is scoped to currently-browsable photos only. Unlocking a folder or turning off "hide sensitive" will need a fresh manual "Index library" run to pick those photos up. This may be an intentional trade-off for a manual, user-triggered index; logged rather than changed.
- `smartAlbumAssets` (`GalleryProjection.kt`, pre-existing, not part of this item's scope) does not exclude archived items for any bucket except its own `ARCHIVED` bucket, so the `Videos`/`Screenshots`/`Favourites`/`Recent` etc. destinations can still show archived photos. `browsableAssets` and `smartAlbumAssets` are therefore not fully equivalent by design; flagging since a future reader might assume otherwise.

Audit performed by a background research pass across `spatial/`, `experience/`, `ai/`, `recognition/`,
`curate/`, `search/` plus the files named directly in the brief; the two real bugs it found
(`BrowserRoute.Tag`, `AlbumsScreen`'s Collections cover) are fixed in this same commit.

## Decisions

- Branch name substitution (above), forced by the harness's fixed per-repo branch assignment.
- (P0-07) `mediaStoreRelativePath` takes an explicit `sdkInt: Int = Build.VERSION.SDK_INT` parameter
  rather than reading `Build.VERSION.SDK_INT` directly, purely so `MediaStoreDestinationTest` can
  exercise both sides of the `Recordings`/API-31 gate. This sandbox has no emulator and only one
  Robolectric framework jar cached offline (confirmed empirically: a `@Config(sdk = [30])` test
  failed on a network fetch this environment cannot make), so simulating a second API level through
  Robolectric itself was not an option here, unlike every other API-gated test in this repo (all of
  which use `sdk = 34` and never assert the pre-gate branch of their own `Build.VERSION.SDK_INT`
  check). Every real caller still just gets the platform's actual value via the default.
- (P0-07) `AudioAsset` gained a `relativePath: String?` field (`= null` default) so
  `AudioConversionWriter` could be wired to `mediaStoreRelativePath` at all — it previously carried
  no `RELATIVE_PATH` from MediaStore whatsoever. Defaulted rather than required, and the scanner
  populates it only from API 29 (matching the photo/video scanner's own existing `RELATIVE_PATH`
  gate); no other construction site exists in `app/src/main` or `app/src/test` for this data class,
  so nothing else needed updating for the new field.
- (P0-08, item 2) "Check what MediaProvider does when a redacted file is opened for writing" —
  the brief's own explicit research ask. `developer.android.com/training/data-storage/shared/media`
  only documents the READ side of `setRequireOriginal`/`ACCESS_MEDIA_LOCATION`, nothing about
  writes. The actual mechanism is `android.os.RedactingFileDescriptor`
  (`android.googlesource.com/platform/frameworks/base/+/master/core/java/android/os/RedactingFileDescriptor.java`),
  whose own javadoc says: "Open the given File and returns a ParcelFileDescriptor that offers a
  redacted view of the underlying data. If a redacted region is written to, the newly written data
  can be read back correctly instead of continuing to be redacted." This confirms redaction is a
  real FD-level wrapper MediaProvider can apply to an opened file regardless of read/write mode,
  and that `ExifInterface.saveAttributes()`'s own read-modify-write cycle (it reads every existing
  attribute before rewriting the whole segment) would read a redacted-away GPS tag as absent and
  then write the segment back without it — a real erasure risk, not a hypothetical one. This
  sandbox cannot inspect MediaProvider's own server-side `openFile` call sites to confirm exactly
  when it chooses to wrap a descriptor this way, so the chosen behavior (refuse entirely without
  `ACCESS_MEDIA_LOCATION` on API 29+, per the brief's own item 2 instruction) stays the fail-closed
  one rather than a narrower one this task could not fully verify.
- (P0-08, item 1) `MetadataWriter.writeBatch` ("bulk metadata edits", named in the brief's item 1)
  has no caller anywhere in `app/src/main` — grepped, confirmed absent, not overlooked. There is
  currently no UI surface for a multi-select metadata batch edit to wire `isMetadataWritable`'s
  pre-flight check into. `write` itself (called by both the single-asset UI path and, internally,
  `writeBatch`) still refuses via item 2's own permission gate regardless, so a HEIC in a
  hypothetical future batch edit would fail safely, just without the "before consent" UX polish
  item 1 asks for specifically. Left as a gap to close when that feature actually gets built.

## Owner questions

1. (P0-01) The similarity index (`ai/SimilarityIndexer.kt`) is now scoped to currently-browsable
   photos only, since its one caller feeds it the same filtered list the map/3D scenes use.
   Unlocking a folder or turning off "hide sensitive" will need a fresh manual "Index library" run
   before those photos show up in the similarity map — unlike the geo and recognition indexers,
   which pre-index everything. Intentional trade-off, or should it index everything like the others?
2. (P0-01) `smartAlbumAssets` only excludes archived items for its own `ARCHIVED` bucket, so the
   `Videos`/`Screenshots`/`Favourites`/`Recent` smart albums and destinations can still show
   archived photos. Pre-existing behaviour, out of this item's scope to change — flagging in case
   it should be reconciled with `browsableAssets` in a later phase.
3. (P0-04, asked verbatim by the brief itself) Animated images and videos are never rendered with
   a frame or watermark — they are stripped and sent as-is (videos via P0-05, not yet done).
   Should a free-tier (non-Pro) sharer's animated or video share still carry the Foto Xplorr
   watermark some other way (e.g. a corner overlay baked into a video's first frame, or an
   animated stamp), or is "no mark on these two formats" acceptable indefinitely?
4. (P0-06, asked verbatim by the brief itself) Zip exports carry no watermark, for every sharer,
   Pro or not — now genuinely enforced (see this row's own notes: it was accidentally *not*
   enforced for a non-Pro sharer before this task's fix, which also meant "keep metadata" zip
   exports were silently dropping GPS they should have kept). Should zip archives carry a watermark
   for a non-Pro sharer, matching a plain share's own Pro-gating, or stay mark-free unconditionally
   as this task now guarantees? The brief's original "never a frame or watermark" line for zip
   predates the Pro/watermark decision by about a week, so it may not have anticipated this
   interaction at all.

## Device checks added

(populated as items land; consolidated into `docs/device-test/phase-0-checklist.md` at P0-19)

- (P0-03) A portrait photo from a camera that writes an EXIF rotation (for example a Samsung)
  opens upright in the editor, saves upright, and shares upright with a frame. The
  `ImageDecoder`-applies-orientation claim this task relies on is empirically confirmed in this
  sandbox (`BitmapDecodingImageDecoderExifTest`, real JPEG + `Orientation=6`, `@GraphicsMode(NATIVE)`)
  but only for a synthetic 4×6 bitmap re-encoded by this same JVM's `Bitmap.compress` — not a real
  camera JPEG's actual encoder quirks, ICC profile, or a HEIC/HEIF file, none of which this
  sandbox can produce or a device-free test can stand in for.
- (P0-04) Share a geotagged JPEG, HEIC and PNG to yourself (or another device) and check each in
  an EXIF viewer for no GPS — the HEIC case specifically exercises the `Unsupported` → re-encode
  path, which this sandbox can construct real bytes for but has no HEIC decoder to confirm against.
  Share a Samsung/Google motion photo and confirm the received file is a plain still JPEG, not the
  original's embedded-MP4-trailer format (this task's JPEG stripper stops at the first EOI
  specifically for this). Share a portrait photo with a Polaroid or stamp frame and confirm it
  arrives upright. Share a GIF or animated WebP with a frame selected and confirm the animation
  still plays on the receiving end rather than arriving as one static, framed image.
- (P0-05) Share a geotagged phone video (with "Remove location and camera data" on): the received
  file has no location (check in a metadata viewer, and separately confirm `MediaMetadataRetriever`
  on it directly if possible), it plays, and it is the right way up — this exercises real
  `MediaExtractor`/`MediaMuxer` behavior this sandbox has no device to confirm at all (no prior test
  in this repo drives them against a real file either). If the source is HEVC, VP8 or VP9, confirm
  the codec-appropriate container came out (MP4 for HEVC, WebM for VP8/VP9) and that it still plays
  in a normal video player. If the phone has an AV1-recorded video, confirm it fails closed with the
  "can't be shared without its location yet" message rather than silently going out unstripped —
  this task could not confirm `MediaMuxer`'s own AV1-in-MP4 support level and deliberately excluded
  it from `containerFor` rather than guess; a device check may reveal AV1 is actually safe to add.
- (P0-06) Select 5 photos (1 HEIC) and 1 video → Zip → share to Files → the archive opens with 6
  entries and no GPS in any of them. Confirm the HEIC entry's extension actually matches its real
  content (it will have been re-encoded to strip its location, per P0-04) and that a zip tool
  reports no compression (STORED-equivalent size) on the entries rather than the smaller size a
  normal DEFLATE archive would show.
- (P0-07) Edit a geotagged camera photo (GPS, date, Make/Model, any existing XMP keywords) and Save
  a copy → the copy sits next to the original in the timeline with the same camera, date and
  location, and its `Orientation` reads normal even if the original camera JPEG's did not. Edit a
  WhatsApp image (`Android/media/com.whatsapp/...`) and Save → the copy lands in
  `Pictures/Foto Xplorr/`, not alongside the WhatsApp original (which the save would otherwise fail
  into). "Replace original" on a photo, then reopen it → metadata and date are unchanged from
  before the edit. Convert or edit a video → check its `RELATIVE_PATH` lands correctly for a
  `DCIM`/`Movies`/`Pictures` source vs. falls back for a `Download` source, and that its date-taken
  matches the source. None of the actual MediaStore insert/`IS_PENDING`/rescan behavior, or whether
  a real device's own MediaStore genuinely re-derives `DATE_TAKEN` from EXIF on rescan (the reason
  `save()` re-asserts it), can be confirmed in this device-less sandbox.
- (P0-08) Turn OFF "Allow access to photo locations" (revoke `ACCESS_MEDIA_LOCATION`) and try to
  edit a caption on any photo → refused with "Allow access to photo locations to edit metadata
  safely", never a consent screen. Turn it back on and retry → succeeds. Add a star rating and a
  few non-English keywords ("café", "北京") to a Lightroom-exported JPEG that already carries its
  own real UTF-8 XMP keywords → every existing keyword survives exactly, unmangled, alongside the
  new rating (this sandbox's `Utf8XmpPreservationTest` proves the decode fix against a hand-built
  packet; a real Lightroom/Capture One export may shape its XMP differently). Open a HEIC or RAW
  photo's metadata controls → disabled with "Foto Xplorr can't write metadata into HEIC files yet"
  the moment the photo is opened, not after tapping a field. Edit a geotagged photo's caption, then
  immediately (before the consent screen or write finishes) edit its rating too → confirm BOTH
  land, not just the second one — this is the one behavior no Robolectric test in this sandbox can
  actually exercise, since it needs the real, timed consent-screen round trip `PhotoDetailRoom`'s
  two independent field commits race against.
- (P0-09) Open a photo in the viewer and rotate the phone → the photo stays open, with no
  "Indexing" banner reappearing. Cold-start the app with a 20k+ item library → one full scan runs
  once, then only deltas for the rest of the session (watch for a SECOND full-progress sweep from
  0, which would mean `ensureInitialScan`'s once-per-process guard failed). Plug in a USB keyboard
  or hub while viewing a photo → nothing resets or reopens the grid. With "Don't keep activities"
  enabled in Developer Options, open a photo, background the app, and let the process die, then
  return to it → the same photo is still open (`rememberSaveable` selection/viewer-list survives;
  `configChanges` alone does not cover this case, only rotation/fold/keyboard/density do). None of
  process death, a real 20k+ item MediaStore library, or real USB keyboard/hub attach events can be
  produced in this device-less sandbox.
